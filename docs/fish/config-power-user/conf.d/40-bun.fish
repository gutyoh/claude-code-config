# 40-bun.fish — Bun (no manual completion sourcing needed for fish)
#
# Bun auto-generates ~/.config/fish/completions/bun.fish on first invocation.
# (~/.bun/_bun is bash/zsh syntax and will error if sourced in fish.)
#
# To force-regenerate:  bun completions
#
# BUN_INSTALL is set in 10-env.fish.
# $HOME/.bun/bin is added to PATH in 00-paths.fish.
