# Fish Config — POWER USER preset (modular layout)
#
# This file is intentionally minimal. All real configuration lives in:
#   conf.d/    — auto-loaded in alphabetical/numeric order on every shell start
#   functions/ — auto-loaded ON FIRST CALL (lazy, zero startup cost)
#
# Source: docs/fish/config-power-user/
# See:    docs/how-to-configure-fish.md
