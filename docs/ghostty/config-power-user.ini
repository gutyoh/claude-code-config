# Ghostty Config: POWER USER (tmux + Neovim Workflow)
#
# Maximum screen real estate with native tabs.
# Optimized for users who live in tmux and use Ghostty as a thin GPU-rendered shell.
# Includes tmux prefix keybindings for common operations.
#
# Install: cp docs/ghostty/config-power-user.ini ~/.config/ghostty/config

# -- Font ---------------------------------------------------------------
font-family = "JetBrainsMono Nerd Font Mono"
font-size = 14
font-style = medium
font-thicken = true
bold-is-bright = true

# -- Theme --------------------------------------------------------------
theme = Catppuccin Mocha

# -- Cursor -------------------------------------------------------------
cursor-style = block
cursor-style-blink = false

# -- macOS --------------------------------------------------------------
# Native titlebar with tabs: keeps traffic light buttons (red/yellow/green).
macos-titlebar-style = tabs
macos-option-as-alt = true
window-save-state = always
confirm-close-surface = false

# -- Window -------------------------------------------------------------
# Slight transparency + blur for depth without sacrificing readability.
background-opacity = 0.9
background-blur = 16
window-padding-balance = true
resize-overlay = never

# -- Shell Integration --------------------------------------------------
# Ghostty caches $SHELL from launchd, so set `command =` explicitly.
# No `~` / env var expansion in path values — swap to match your install:
#   macOS Apple Silicon: /opt/homebrew/bin/fish
#   macOS Intel:         /usr/local/bin/fish
#   Linux brew:          /home/linuxbrew/.linuxbrew/bin/fish
#   Linux pkg mgr:       /usr/bin/fish
#
# Commented out so this preset is portable as shipped: an absolute path
# baked in here breaks every machine whose fish lives elsewhere. Uncomment
# the line matching your install.
# command = /opt/homebrew/bin/fish
shell-integration-features = cursor,sudo,title,ssh-env,ssh-terminfo

# -- Clipboard ----------------------------------------------------------
# Allow read/write for tmux/neovim clipboard integration (OSC 52).
copy-on-select = clipboard
clipboard-trim-trailing-spaces = true
clipboard-read = allow
clipboard-write = allow

# -- Mouse --------------------------------------------------------------
mouse-hide-while-typing = true
mouse-scroll-multiplier = 2

# -- Performance --------------------------------------------------------
# Large scrollback for log tailing. 10M lines for heavy use.
scrollback-limit = 10_000_000

# -- Behavior -----------------------------------------------------------
quit-after-last-window-closed = true
auto-update = check

# -- Quick Terminal -----------------------------------------------------
keybind = global:super+grave_accent=toggle_quick_terminal
quick-terminal-position = top
quick-terminal-animation-duration = 0

# -- tmux Keybindings ---------------------------------------------------
# Assumes tmux prefix is Ctrl+a (\x01).
# Cmd+S -> tmux save-buffer
keybind = super+s=text:\x01\x73
# Cmd+B -> tmux zoom toggle
keybind = super+b=text:\x01\x7a

# -- Utility Keybindings ------------------------------------------------
keybind = shift+enter=text:\n
keybind = super+shift+,=reload_config
